# Shared default properties for all services registered with Spring Cloud Config

logging.level.root=INFO
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Example shared DB defaults (override in each service as needed)
# spring.datasource.username=root
# spring.datasource.password=patan
